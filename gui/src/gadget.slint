// Shared UI struct for gadgets, matching the Rust Gadget struct in gogo-core
// Note: In Rust, 'command' is a Command struct, but here it is a string for Slint compatibility
export struct GadgetVariable {
    name: string,
    description: string,
    default: string,
}

export struct Gadget {
    name: string,
    command: string, // maps to Command.raw in Rust
    description: string,
    variables: [GadgetVariable],
}
